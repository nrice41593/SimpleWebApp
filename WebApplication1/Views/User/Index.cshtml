@model WebApplication1.Models.UserModel
@{
    ViewBag.Title = "Index";
}

<h2>User Page</h2>

@using (Html.BeginForm("Index", "User", FormMethod.Post, new { id = "userForm" }))
{
    <table>
        <tr>
            <td>ID:</td>
            <td><input id="ID" type="number" name="ID" required /></td>
        </tr>
        <tr>
            <td>First Name:</td>
            <td><input id="FirstName" type="text" name="FirstName" required /></td>
        </tr>
        <tr>
            <td>Last Name: </td>
            <td><input id="LastName" type="text" name="LastName" required /></td>
        </tr>
        <tr>
            <td>Age: </td>
            <td><input id="Age" type="number" name="Age" required /></td>
        </tr>
        <tr>
            <td>Register Date: </td>
            <td><input id="RegisterDate" type="datetime" name="RegisterDate" required /></td>
        </tr>
        <tr>
            <td><input type="submit" value="Submit"/> <input id="ajaxSubmit" type="button" value="Ajax Submit" /></td>
        </tr>
    </table>
}

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js">
        $("#ajaxSubmit").submit(function (e) {
            var valdata = $("#userForm").serialize();
            alert(valdata);
            $.ajax({
                url: "/User/Test",
                type: "POST",
                dataType: 'json',
                contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
                data: valdata
            });
        });
</script>